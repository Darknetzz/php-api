# Protect config, keys, data, and CLI scripts from direct HTTP access
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(keys|settings|lib|data|bin|endpoints|aliases)/ - [F,L]
</IfModule>
